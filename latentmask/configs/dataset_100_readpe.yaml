# Dataset 100: READ-PE (40 cases, 5-fold CV)
# Primary internal benchmark

dataset:
  id: 100
  name: Dataset100_READPE
  n_cases: 40
  fold: all  # Run all 5 folds
  config: 3d_fullres

# Auxiliary data
auxiliary:
  rspect_manifest: ${nnUNet_raw}/manifests/rspect_manifest.csv
  aug_rspect_manifest: ${nnUNet_raw}/manifests/aug_rspect_manifest.csv
